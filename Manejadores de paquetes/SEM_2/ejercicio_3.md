Sin instalar nada de forma permanente, usa npx para ejecutar la herramienta cowsay y que una vaca diga "Aprendiendo npm". Luego explica por qué npx fue mejor que instalar globalmente para esto.

===================================

1. En un proyecto que ya tenga dependencias instaladas, borra node_modules y restáuralo usando npm ci. 
   Observa el mensaje que devuelve.
2. Usa npx para crear un proyecto nuevo con create-vite (elige la plantilla "Vanilla"). No instales create-vite de forma permanente.