// Crea un objeto que tenga datos y también algo que "sabe hacer".

// Requerimientos

// Objeto circulo con propiedad radio.
// Método area() que retorne π × radio² (usa Math.PI).
// Método perimetro() que retorne 2 × π × radio.

const readline = require("readline-sync");