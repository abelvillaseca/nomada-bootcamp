// Crea un arreglo con los nombres de 5 ciudades y muestra en consola cada ciudad con su posición.

let ciudades = ["Madrid", "Barcelona", "Valencia", "Sevilla", "Zaragoza"];

console.log("Ciudades y sus posiciones:");

for (let i = 0; i < ciudades.length; i++) {
    console.log(`${i}: ${ciudades[i]}`);
}