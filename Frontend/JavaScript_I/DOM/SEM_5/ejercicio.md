DOM

Cambia el valor de un campo de entrada con getElementById​
Cambia una imagen a otra al hacer clic en un botón​
Muestra en pantalla cuántos elementos <li> tiene una lista​
Crear un h1 con class=“titulo”. Usa getElementsByClassName para cambiar su color a rojo cuando se haga clic en un botón​
Selecciona el primer elemento con la clase "elementoDestacado" usando querySelector y cambia su color de fondo a amarillo.​
Selecciona todos los enlaces (<a>) en el documento usando querySelectorAll y muestra la URL de cada enlace en la consola.​

Selecciona todos los elementos <p> que tienen la clase “destacado” usando querySelectorAll y cambia su tamaño de fuente a 18px​
Verifica si el elemento con el ID “miElemento” coincide con el selector CSS ".miClase". Usa matches()​
